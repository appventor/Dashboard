import '../../../pages.dart';

class OfferDetails extends StatelessWidget {
  final String id;
  const OfferDetails({Key? key, @PathParam('id') required this.id})
      : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Container();
  }
}
