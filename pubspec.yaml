name: dashboard
description: Flutter admin dashboard app.
publish_to: "none"
version: 0.0.2

environment:
  sdk: ">=2.12.0 <3.0.0"

dependencies:
  flutter:
    sdk: flutter
  auto_route: ^1.0.2

dev_dependencies:
  change_app_package_name: ^0.1.2
  auto_route_generator: ^1.0.2
  build_runner:
  flutter_test:
    sdk: flutter

flutter:
  uses-material-design: true
